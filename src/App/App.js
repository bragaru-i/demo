import React from 'react';

import MainPage from '../MainPage/MainPage';
import Layout from '../Layout/Layout';

const App = (props) => {
  return (
    <>
      <Layout>
        <MainPage />
      </Layout>
    </>
  );
};

export default App;
